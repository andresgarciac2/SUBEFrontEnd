import { CurrentStepController } from './currentstep.controller';

export const CurrentStepComponent: angular.IComponentOptions = {
    templateUrl: 'app/postulation_module/components/currentstep/currentstep.html',
    controller: CurrentStepController,
    controllerAs: 'vm',
    bindings: {
    }
};