import { PostulationtController } from './postulation.controller';

export const PostulationComponent: angular.IComponentOptions = {
    templateUrl: 'app/postulation_module/components/postulation/postulation.html',
    controller: PostulationtController,
    controllerAs: 'vm',
    bindings: {
    }
};
